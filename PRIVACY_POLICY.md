# سياسة الخصوصية (Privacy Policy)

**تطبيق كاشف بوابات الدخول (Captive Portal Detector)**

تاريخ السريان: 25 سبتمبر 2026

تلتزم إدارة تطبيق "Captive Portal Detector" بحماية خصوصية المستخدمين بشكل كامل. يوضح هذا المستند كيفية التعامل مع البيانات والصلاحيات داخل التطبيق.

## 1. جمع البيانات واستخدامها
- **لا نقوم بجمع أي بيانات شخصية:** التطبيق لا يجمع، ولا يخزن، ولا يشارك، ولا ينقل أي بيانات تعريف شخصية (PII)، أو بيانات موقع، أو سجلات تصفح إلى أي خوادم خارجية.
- **تخزين محلي فقط:** جميع سجلات الأحداث وتغيرات حالة الشبكة تُحفظ محلياً على جهاز المستخدم داخل قاعدة بيانات مشفرة بحد أقصى للأحداث، ويمكن للمستخدم مسحها بالكامل في أي وقت بنقرة واحدة.

## 2. الصلاحيات المطلوبة والغاية منها
- **الوصول للإنترنت وحالة الشبكة (`INTERNET`, `ACCESS_NETWORK_STATE`, `ACCESS_WIFI_STATE`):**
  تُستخدم حصراً للتحقق من الاتصال بالإنترنت واكتشاف صفحات تسجيل الدخول إلى الشبكات (Captive Portals).
- **الإشعارات (`POST_NOTIFICATIONS`):**
  تُستخدم لتنبيه المستخدم فور رصد شبكة تتطلب تسجيل الدخول، بالإضافة إلى عرض إشعار خدمة المراقبة الدائمة.
- **خدمات الخلفية (`FOREGROUND_SERVICE`, `FOREGROUND_SERVICE_DATA_SYNC`):**
  تُستخدم لضمان استمرار مراقبة الشبكة وتغيراتها عند التنقل والاتصال بشبكات جديدة أثناء تشغيل الخدمة.
- **إعادة التشغيل (`RECEIVE_BOOT_COMPLETED`):**
  تُستخدم لجدولة فحص الاتصال تلقائياً عند إعادة تشغيل الجهاز دون الحاجة لفتح التطبيق يدوياً.

## 3. المتصفح المدمج والمصادقة
- يتم فتح صفحات تسجيل الدخول إلى شبكات الواي فاي عبر متصفح داخلي مقيّد بالشبكة المعنية (`bindProcessToNetwork`).
- التطبيق لا يعترض أو يخزن أو يقرأ بيانات الاعتماد أو كلمات المرور أو أرقام الغرف المدخلة في صفحات بوابات الدخول.

## 4. التواصل
لأي استفسارات أو ملاحظات بخصوص سياسة الخصوصية أو الكود المصدري، يرجى التواصل عبر مستودع المشروع الرسمي على GitHub.
